import React from 'react'
import { Box, Typography, Button } from '@mui/material'

const Quiz = () => {
  const questions = [
    { question: 'What is 1+1 ?', options: ['3', '4', '2'], answer: '2' },
    { question: 'What is the capital of France?', option: ['Paris', 'London', 'Rome'], answer: 'Paris'}
  ]

  return (
    <Box>

    </Box>
  )
}

export default Quiz